/**
 * linkedin-post-spy — email tool module. meta: listing/page copy + identity + gate config.
 * The execution pipeline is shared Convex infrastructure (email tools are
 * parameterized instances of it); per-tool pipeline config joins this module
 * when the Convex side is wired up.
 */

import { meta } from "./meta";

export { meta };
